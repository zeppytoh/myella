---
// Assets
import Logo from "@/components/assets/Logo.astro";
// Fundations
import Button from "@/components/foundations/elements/Button.astro";
import Wrapper from "@/components/foundations/containers/Wrapper.astro";
// Icons
import X from "@/components/foundations/icons/X.astro";
import Menu from "@/components/foundations/icons/Menu.astro";

const navigationLinks = [
  { title: "Overview", href: "/system/overview" },
  { title: "Blog", href: "/blog/" },
];
---

<div id="nav-wrapper" class="fixed inset-x-0 top-0 z-50 w-full mx-auto">
  <Wrapper
    id="second-wrapper"
    variant="standard"
    class="py-4 border-black border-y-2 bg-accent-200"
  >
    <div
      id="navigation-wrapper"
      class="relative flex flex-col w-full md:items-center md:justify-between md:flex-row"
    >
      <div class="flex flex-row items-center justify-between">
        <a href="/">
          <span class="sr-only">Go to homepage</span>
          <Logo class="text-lg font-medium uppercase text-base-800" />
        </a>
        <Button
          iconOnly
          size="sm"
          variant="default"
          id="menu-toggle"
          class="md:hidden"
        >
        <Menu slot="icon" size="base" />
          
        </Button>
      </div>

      <nav
        id="navigation-menu"
        class="fixed inset-0 flex flex-col justify-between h-full py-12 bg-white opacity-0 pointer-events-none md:bg-none md:bg-transparent lg:p-0 md:p-0 transform transition-all duration-300 ease-in-out -translate-y-4 md:relative md:inset-auto md:opacity-100 md:pointer-events-auto md:translate-y-0"
      >
        <div
          class="absolute inset-0 mx-2 pointer-events-none bg-accent-200 border-x border-base-200 md:hidden"
        >
        </div>

        <Button
          iconOnly
          size="sm"
          variant="default"
          id="menu-close"
          class="absolute top-4 right-4 md:hidden"
          aria-label="Close menu"
        >
          <X slot="icon" size="base" />
        </Button>

        <div
          class="relative flex md:bg-transparent flex-col items-start justify-start w-full h-full px-8 list-none md:ml-auto gap-12 md:px-0 md:flex-row md:items-center md:justify-center md:text-left md:gap-4"
        >
          <a href="/" class="md:hidden">
            <span class="sr-only">Go to homepage</span>
            <Logo class="h-12 text-base-800" />
          </a>

          <div class="flex flex-col gap-2 md:gap-4 md:flex-row">
            {
              navigationLinks.map((link) => (
                <a
                  href={link.href}
                  class="text-2xl font-medium md:text-sm hover:text-base-800 text-base-900"
                >
                  {link.title}
                </a>
              ))
            }
          </div>

          <div class="flex flex-wrap items-center mt-auto gap-2">
            <Button
            isLink="true"
              size="sm"
              variant="default"
              href="https://buy.polar.sh/polar_cl_0IdNP4xCBbKSEBvkVTcnJAuDo2xtuDwTeMpBj3IdlFI"
            >
              Buy Ellamae
            </Button>
          </div>
        </div>
      </nav>
    </div>
  </Wrapper>
</div>

<script type="module">
  document.addEventListener("DOMContentLoaded", () => {
    const menuToggle = document.getElementById("menu-toggle");
    const menuClose = document.getElementById("menu-close");
    const menuIcon = document.getElementById("menu-icon");
    const closeIcon = document.getElementById("close-icon");
    const navigationMenu = document.getElementById("navigation-menu");

    function toggleMenu(open) {
      navigationMenu.classList.toggle("opacity-100", open);
      navigationMenu.classList.toggle("translate-y-0", open);
      navigationMenu.classList.toggle("pointer-events-auto", open);
      navigationMenu.classList.toggle("opacity-0", !open);
      navigationMenu.classList.toggle("-translate-y-4", !open);
      navigationMenu.classList.toggle("pointer-events-none", !open);
      menuIcon.classList.toggle("hidden", open);
      closeIcon.classList.toggle("hidden", !open);
    }

    menuToggle.addEventListener("click", () => toggleMenu(true));
    menuClose.addEventListener("click", () => toggleMenu(false));
  });
</script>
